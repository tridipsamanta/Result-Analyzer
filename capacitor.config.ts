import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tridip.resultanalyzer',
  appName: 'Result Analyzer',
  webDir: 'dist'
};

export default config;
